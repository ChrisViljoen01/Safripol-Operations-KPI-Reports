# Credentials for the Safripol ops report ingest.
# LOCAL ONLY - this file is gitignored and must never be committed or emailed.
# App registration: "Connect Logistics AI Hub"

AZURE_TENANT_ID=
AZURE_CLIENT_ID=

# Paste the secret VALUE here (not the Secret ID). Azure shows it once only.
AZURE_CLIENT_SECRET=

# Fallback folder used when Graph is unreachable.
SAFRIPOL_LOCAL_DIR=
